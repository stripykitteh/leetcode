class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        # iterate over all strings, bail as soon as one string is not congruent
        # with the first string
        for i in str:
            print(i[0])
        


if __name__ == '__main__':

    strs = ["flower","flow","flight"]
        
    Solution().longestCommon_Prefix(strs)
    print(Solution().longestCommon_Prefix(strs))
 
